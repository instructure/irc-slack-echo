/*eslint-env mocha */
var expect = require('chai').expect;
var quietTimes = require('../lib/quietTimes');

describe('Quiet Times', function () {
  describe('isInQuietTimes', function () {
    xit('reflects proper quiet times based on the config', function () {

    });
  });
});
